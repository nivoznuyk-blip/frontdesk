import { PageStub } from '@/components/layout/PageStub';

export default function WidgetBuilder() {
  return (
    <PageStub title="Widget builder">
      Settings on the left, a live preview on the right, and the embed code below.
    </PageStub>
  );
}
