import { PageStub } from '@/components/layout/PageStub';

export default function Sources() {
  return (
    <PageStub title="Sources">
      One row per source, with pages, chunks, status and the actions to reindex or remove it.
    </PageStub>
  );
}
