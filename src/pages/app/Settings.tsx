import { PageStub } from '@/components/layout/PageStub';

export default function Settings() {
  return (
    <PageStub title="Settings">
      Plan and usage, billing history, team members, and the danger zone.
    </PageStub>
  );
}
