import { PageStub } from '@/components/layout/PageStub';

export default function Playground() {
  return (
    <PageStub title="Playground">
      The conversation on the left, sources and answer controls on the right.
    </PageStub>
  );
}
