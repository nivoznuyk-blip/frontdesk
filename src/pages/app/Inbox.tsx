import { PageStub } from '@/components/layout/PageStub';

export default function Inbox() {
  return (
    <PageStub title="Inbox">
      Conversations from the widget, with filters and the form for correcting a bad answer.
    </PageStub>
  );
}
